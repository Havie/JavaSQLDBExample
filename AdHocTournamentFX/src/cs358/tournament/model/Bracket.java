package cs358.tournament.model;

import java.util.ArrayList;

public class Bracket {
  
  /*
  public ArrayList<Round> mRounds;
  
  public Game findGame(String gameId) {
     
    for (int i = 0; i < mRounds.size(); i++) {
      
      if(mRounds.get(i).findGame(gameId) != null) 
        return mRounds.get(i).findGame(gameId);    
    }
    
    return null;  
  }
  
  public String toString() {
    
    String theBracket = "";
    
    for(Round e: mRounds) {
      
      theBracket += e.toString() + "/n";
      
    }
    return theBracket;
  }*/

}
